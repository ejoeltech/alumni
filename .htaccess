<IfModule mod_rewrite.c>
    RewriteEngine on
    # If the URL already starts with public/, stop processing and pass it right through.
    RewriteRule ^public/ - [L]
    # Otherwise, forward everything else silently into the public/ directory.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
